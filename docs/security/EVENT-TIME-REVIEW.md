# Candidate1.6 event timestamp source review

Runtime source review passed; six focused tests passed (four owner fixtures and two independent malicious-input/fixture-configuration cases). Source hashes and log: `evidence/event-time-security-review.json`, `event-time-security-tests.log`. This is not image/audit/browser/deployment acceptance. Deployed1.5 and its immutable manifest remain separate; no dependency or image rescan was performed.

The helper reads only canonicalUTC created, checks exact four-digit/millisecond/Z syntax and Date roundtrip calendar validity, and uses explicit Asia/Seoul formatting. Malformed/nonstring values return null without coercion or raw echo; alternate time fields never fill gaps. React time datetime/title/aria attributes use only validated values; visible KST and originalUTC are both labelled. The diff changes only event table formatting; the existing shared time formatter and other screens are untouched. React text rendering does not interpret event message HTML. Timestamp validation is not a scrubber for the independent existing event message column.

The acceptance script requires explicit reviewed1.6/mode/exactloopbackURL/bundlehash/private token path/fresh evidence directory, reads existing events, and uses browser-only synthetic GET/SSE fulfillment. No server event creation is present. Token guard checks body/HTML/result for the current token, with screenshots taken after DOM check. Raw event messages remain in the evidence for equality comparison; this is not an unknown-sensitive-data guarantee. Private path validation is lexical under a trusted local operator and is not a hostile symlink boundary.

Initial fixture hygiene correction requested from issues/root (resolved in follow-up below): top-level console.error(e.message) may expose raw assertion mismatches, including event message text. Replace with a fixed failure diagnostic before treating the harness as safe to publish failure output. Source is owned by issues, not modified by this reviewer. Request timeouts exist for the initial state fetch; full browser execution must retain its external bounded operator wrapper. No remote write or process operation was performed during this review.

Follow-up: frozen harness SHA256 dc4309cb4651036967acab2bb6954551c609f99620ba9d5876a9878cdc734743 now prints only a fixed failure message. An isolated missing-configuration invocation exits1 without the thrown raw reason and never starts a browser. Non-GET routes explicitly abort and increment a counter checked as zero before PASS; all-context page errors and awaited asset tasks are present. The prior failure-output finding is resolved. Initial source hashes remain historical, with the new hash recorded under followup. No full suite, scan or actual browser/remote operation was repeated by this reviewer.
