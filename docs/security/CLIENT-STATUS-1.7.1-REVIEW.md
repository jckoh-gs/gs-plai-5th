# Candidate1.7.1 client status correction

Bounded source gate passes; four client-security tests independently pass. The only readMessage DTO change adds online as strict boolean for status topics. False is preserved; missing/null/string/number values become null. Other topic kinds cannot use online to override command status. Existing size cap, topic/RTU binding, JSON object validation, command status allowlist, retained ack/command-status rejection and text redaction functions remain unchanged. This preserves existing sanitizer behavior, not a new guarantee that all unknown secrets are removed.

The protocol now distinguishes example Compose18883/app3101 from runtime1883/app3001 defaults, preserves existing .env rather than overwriting it, and removes unsupported separate-v1 legacy_accepted/migration claims. Retained online/LWT offline is correctly described as received/broker-held state rather than fresh device-health or missed-session delivery proof. No new remote command, credential publication or automatic control retry is introduced by these source/doc changes.

All non-root package-lock entries remain byte-equivalent as parsed objects to d98d3c4/1.7. Root version metadata is the candidate change. This review does not run a fresh npm audit or exact OCI scan, and it does not claim the new actual client-status integration ran. Root owns that integration execution. Actualstable1.7 remains separate from this1.7.1 candidate. Source hashes and test log: evidence/client-status-1.7.1-source-review.json and client-status-1.7.1-source-tests.log. No blocking regression found in the narrow reviewed delta; no source/runtime/remote mutation by this reviewer.

Quickstart follow-up: docs now wrap cp -n with `if [ ! -e .env ] && [ ! -L .env ]; then ...; fi`. Existing files and symlinks, including dangling symlinks, skip the copy; this avoids treating macOS cp -n's existing-destination exit1 as a failed setup without overwriting configuration. This is a local trusted-operator convenience, not a race-proof filesystem security boundary.

Read-only review of candidate-1.7.1-local/quickstart.json confirms its bounded claim: fresh private source/dependency/env/DB path,3 connected demo RTUs, explicit3112 port override, unchanged original environment, owned app stopped/port closed. The already-owned18883 broker was reused with no-recreate; this is not a new clean broker installation. The first occupied3101 preflight and subsequent cp -n failure remain historical failed attempts, not relabelled successful. This reviewer did not rerun quickstart. Original source-review hashes remain historical and new documentation/receipt hashes are appended under quickstartFollowup. Runtime/remote state and package contents were not changed by this review.
