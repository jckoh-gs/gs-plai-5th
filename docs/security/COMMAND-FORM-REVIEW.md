# Command form candidate1.4 security review

Bounded independent review passed:44 isolated tests including5 command-form fixtures,3 independent adversarial/boundary fixtures and36 final-deck binder fixtures. Evidence: `evidence/command-form-tests.log`, source/lock hashes in `evidence/command-form-review.json`, fresh full npm audit in `evidence/command-form-npm-audit.json`. All non-root lock package entries are identical to deployed3fa3ba0; no added dependency or package-version change. Audit reports0 vulnerabilities; this is not an OS/image scan or whole-product safety claim. No candidate1.4 OCI scan or remote browser acceptance is claimed here.

The helper rejects empty, nonnumeric, nonfinite and out-of-range inputs using fixed Korean field messages without reflecting supplied input. Tolerance0.01–10000, timeout1–3600 and priority0–100 match server validation. Fractions, target0 and availability-exceeding targets remain unchanged; server retains rated-capacity, generator, expiry and authorization authority. Date overflow and expanded years are caught before act. Boundary9999 is accepted and10000 rejected. Client now is supplied once at submit. Invalid fields return before commandId creation/POST; field focus, alert and aria linkage are present. Lab keyed by RTU id remounts Target and its error state on RTU selection change. Browser behavior is separately owned by UI review, not inferred as executed by these unit tests.

No new known-secret exposure path was found: helper messages contain no input values, credentials are not read by the helper, and error text is rendered through React. Generic existing API error handling is not newly certified by this narrowly scoped change. Validation is UI convenience, not a security boundary; direct HTTP/MQTT callers still require server checks.

Existing limitation, observed from source only: Target has no concurrent-submit lock and creates a fresh command UUID for each valid submission. Rapid duplicate actions can therefore represent separate commands; no two-POST browser reproduction was performed here. This differs from server idempotency for the same commandId/body. Root explicitly retained this pre-existing UX behavior outside the adopted range/date change; it is not treated as a new blocking regression or added requirement.

The binder delta additionally permits exact1.4.0 while retaining prior1.0–1.3 allowance and rejecting unsupported future versions. Existing identity, reviewed-facts, timestamps, evidence paths, secret text and recovery checks remain unchanged;36 binder tests passed. This enables candidate-compatible preparation only, not a generated or reviewed final video/deck.
