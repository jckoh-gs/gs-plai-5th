# Candidate1.7 command list freshness security review

Source/focused-test gate passed: five owner fixtures independently rerun, all pass. Source and package hashes are in `evidence/command-list-freshness-review.json`; execution log is `command-list-freshness-tests.log`. Non-root package-lock entries are identical to331ab9a, so no added OSS was found. This is not a fresh npm/image audit, browser acceptance or deployed1.7 claim; existing image findings remain unchanged.

Initial source review found that unmount cleared the poll timer and aborted the request but left its local deadline timer alive until resolution/timeout. UI owner moved that timer into the poll lifetime and now clears it synchronously on stop. The final test verifies zero owned timers even when a fake read ignores abort and resolves late. Body completion also checks signal.aborted before accepting rows.

The reader performs only authenticated GET, encodes RTU identity in the path, refuses redirects and does not parse failed HTTP response bodies.401 invokes the existing auth-required login event. Errors rendered by this new list status are fixed text or numeric HTTP status, without raw response/error messages. The15second Promise boundary covers response-body waiting; timeout aborts the owned request and late results do not update the list. A completed attempt schedules its next poll after the existing2second interval; there is no POST or control retry. Browser abort is not a guarantee of remote request cancellation or timely execution during host suspension.

Effects bind to p.id, stop the old poll on change/unmount, and stamp list state with its RTU id. Render additionally hides a state whose plantId differs from current p.id, preventing a pre-effect stale display. Loading, successful-empty, failure with retained rows and browser-success timestamp remain distinct; success replaces rows/time and clears warning. Status/alert roles and the distinction from SSE/server command time are explicit. Existing row order/20limit, command fields, export and receipt logic are not redesigned. React still renders values as text; the existing command reason/ID fields are not newly scrubbed or certified free of arbitrary sensitive data by this change.

Limits: array/object shape validation is not a complete untrusted DTO sanitizer or a response-byte cap; it consumes the authenticated same-origin server contract. An abort-ignoring custom transport can remain pending even after client timeout, whereas actual browser fetch is expected to honor abort. Focused mocks establish local state/timer behavior, not real remote network or browser acceptance. No remote writes, source edits, full-image scan, broker/registry rescan or commits were performed by this review.

Final source follow-up: src SHA ffbb204d9275fc14ebfdd9ea3ea488494ca2c0c97271f6af69f69993ddbfa592 and helper SHA18e353fb7119f39724209f662458bab82e5737e827ad1013529cdfff3011f026 match the reported frozen revision. Live status text now changes by meaningful loading/success-empty/success/failure phase rather than every2second timestamp; changing success time/loading detail is outside the live region. Helper catch tolerates null/undefined errors while retaining fixed diagnostics. Related focused poll plus form/receipt/event target guards total22 tests independently pass. Explicit1.7 candidate routes use3111, wrong main/older-candidate targets are refused by mutating fixtures, and event main mode remains explicitly read-only. Historical hashes remain intact under the receipt's original fields; finalFollowup holds current hashes. No broad suite or image scan was repeated.
