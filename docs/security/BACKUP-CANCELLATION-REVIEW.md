# ISSUE024 backup cancellation review

Incident evidence records a90second host exec timeout with an incomplete remote snapshot, followed by a liveness-triggered app restart. Internal stall cause remains unknown; overlap with backup is not a proven causal diagnosis. Original stable backup is retained and partial output is not a successful snapshot. This review does not change that evidence or run a remote backup.

The proposed worker opens the source read-only, requires WAL, starts a read transaction and reads schema to fix its snapshot before backup. Progress performs absolute and monotonic time checks. Source transaction is rolled back and closed before destination integrity checking. The parent creates a wx0600 partial, spawns only its own ChildProcess with ignored stdout/stderr and fixed-stage/numeric IPC, and alone hardlinks the final path after verified IPC, confirmed exit0 and deadline checks. Existing final paths fail before worker start. Exit-confirmed/exitCode/signalCode guards prevent routine signalling after a reaped worker. No broad PID search/process-group kill exists.

Three independent isolated fixtures pass: verified IPC followed by exit1 cannot publish a final and worker raw output stays hidden; existing final content remains unchanged; a synchronous noncooperative worker is terminated and no final is published. Test/source hashes and log are in `evidence/backup-cancellation-independent-review.json` and `backup-cancellation-independent-tests.log`. Owner concurrent-writer proof is separate; these tests do not claim a production snapshot or availability recovery.

Pending owner follow-up at this initial source snapshot: after SIGKILL request, parent awaits child exit without a final settlement timer. Uninterruptible OS I/O or signal failure can therefore keep the remote parent beyond its nominal budget. An error event also rejects that wait and clears timers; successful-spawn/error cleanup must not strand its worker. These are reported design boundaries, not reproduced actual kernel stalls. Parent bounded termination and worker termination confirmation must be distinguished.

Further limits: progress callbacks cannot interrupt synchronous SQLite integrity I/O; a separate running parent can request termination but SIGKILL/host failure/unscheduled parent cannot guarantee cleanup. Long source read snapshots can retain WAL pages, so their bounded lifecycle matters and is not proof of no writer resource impact. Final deadline check and hardlink have a small unavoidable scheduling gap. Existing METADATA_SOURCE synchronous integrity/compression lifecycle is outside this snapshot-worker change; host kubectl timeout remains insufficient to prove cancellation there. No raw DB payload/credentials or remote changes were introduced by this review. Parent/root must approve the final operational boundary before reuse.

## Final source review

The initial parent-wait finding is resolved in the final reviewed source. A final allowance timer emits fixed INCOMPLETE/cleanup-unconfirmed and exits1 when child termination cannot be confirmed. SIGTERM/SIGINT request termination only of the tracked live child, then escalate within the original remaining allowance. Child error without a spawned PID fails immediately; errors for an existing worker retain the final deadline rather than cancelling all watchdog coverage. Successful publication still requires confirmed exit0 and verified IPC; unconfirmed cleanup never authorizes final publication. These are scheduling-dependent parent bounds, not a guarantee against parent SIGKILL, host failure or uninterruptible kernel work.

Independent final rerun: own three adversarial fixtures pass; additionally the owner's parent-SIGTERM and non-WAL fixtures pass when selectively executed. The owner separately records local15 and exact4af Node24.21 Linuxx64 isolated15 PASS; initial two startup-timing fixture failures remain preserved and are not presented as application failures or silently removed. This reviewer did not repeat the entire suite or image tests.

The entry point parses snapshot JSON only after successful subprocess completion and requires result=PASS, workerExitConfirmed=true, integrity=ok before recording the private journal snapshot and advancing to metadata. The internally generated supervisor emits fixed phases/booleans/numeric metadata; worker stdout/stderr are discarded and unsuccessful host commands withhold output. Spread into the private journal is trusted supervisor output, not an externally authenticated arbitrary DTO. No API/DB contents or credentials are deliberately included in this new output. Failed source jobs leave partials, preserve previous finals and produce no final operation PASS.

Final source/evidence hashes and selected-test logs are recorded under finalReview in the existing independent receipt; initial hashes remain historical. Source gate passes with the listed METADATA_SOURCE, external-SIGKILL, kernel-I/O, WAL-retention and hardlink timing limits. This is not authorization for a production retry and does not establish the original stall's root cause. Runtime1.6 image4af is unchanged by these operational tools.

Final entry wording follow-up: existing-final retransfer now records snapshot:null / existing-final-retransfer and explicitly does not re-establish how the old snapshot was captured. Newly supervised captures alone receive new-supervised/fixed-WAL wording. This small entry-only change was source-inspected after the selected tests; helper execution is unchanged and the new source hash is recorded separately without relabelling earlier test hashes.

Full-suite diagnostic-contract follow-up: root's preserved full-unit round1 recorded290 total/289 PASS/1 FAIL. The sole failure was the independent expired-snapshot test still expecting old stderr text `/deadline expired/`; the new helper intentionally emits fixed JSON stdout and empty stderr. Its later no-file assertions were not reached in that failed execution. The test-only expectation now checks INCOMPLETE, workerExitConfirmed=false, stage=starting, empty stderr, absence of raw paths/fixture marker, and absence of both final and partial files. Independent review-backup.test.js rerun4/4 PASS; helper source unchanged. The original full-suite failure is not hidden or relabelled. Evidence: backup-diagnostic-contract-tests.log and backup-diagnostic-contract-review.json. Root owns the subsequent full-suite round2.

Timing fixture follow-up: preserved full-unit round2 also recorded289/290 PASS, now failing the hung-discovery marker assertion. The marker was absent after approximately814ms against an800ms original fixture budget; process startup consuming that budget is a plausible explanation, not conclusively proven. The test retains real CLI execution, actual fake-kubectl-start proof, nonexpired/expired branch distinction, secret-output absence, incomplete discovery journal and no-result checks. Only synthetic startup allowance increased to2500ms, with6000ms subprocess timeout and5000ms elapsed ceiling (deadline plus bounded cleanup tolerance). Independent file rerun4/4 PASS. Helper/runtime remain unchanged; original round2 failure stays preserved and root owns full round3.

## Outbox proof integration follow-up

Independent integration source gate passed with5 focused tests: remote-outbox-backup2 plus independent worker invariants3. The outbox capture now imports the exact shared SNAPSHOT_SOURCE rather than maintaining an unbounded duplicate backup implementation. It revalidates the same Ready pod before capture, execs that verified pod by name, passes the unchanged original stopAt, and retains80second supervisor/90second host caps. PASS, confirmed worker exit, integrity ok, verified stage and numeric snapshot timestamp are required before rollout restart. Failed/incomplete capture cannot advance to restart. Snapshot metadata is internal fixed supervisor output; raw DB bodies are not introduced into the new result. Existing query/MQTT raw body comparison stays in memory and output retains its hash.

Evidence is `outbox-supervised-snapshot-review.json` and `outbox-supervised-snapshot-review-tests.log`; no remote execution or full-suite rerun occurred. Existing limits remain: host cancellation cannot guarantee remote cleanup; the final fault-restoration call uses remaining original budget and can fail after deadline. The existing proof result is written before that finally cleanup; callers must inspect process exit and final normal-state proof, not result-file existence alone. The new capture does not expand that old result into a full cleanup guarantee. Parent watchdog/metadata/kernel/WAL limits described above still apply. No runtime image or ownership ledger changed.
