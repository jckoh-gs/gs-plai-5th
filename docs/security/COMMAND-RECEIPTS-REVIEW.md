# Command receipt candidate1.5 independent security review

Source and isolated helper gate passed after two focused corrections:11 tests passed. Initial independent tests recorded two failures, retained in `evidence/command-receipts-security-initial.log`; the final combined run is `command-receipts-security-final.log`. Source SHA256 and exact scope are in `command-receipts-security-review.json`. No browser acceptance, candidate image scan, deployed1.5 result or remote mutation is claimed. Existing1.4 deployment remains separate. No dependency/image rescan was performed for this source-only review.

The initial request helper omitted redirect policy, allowing browser-default307/308 POST replay. UI owner added redirect:error; the independent fixture checks the fetch option and verifies non-2xx bodies are not parsed. The initial reason filter removed only the current token, leaving credential-bearing URLs reflected through superseded MQTT command IDs. UI owner now removes controls first, redacts whole credential-bearing URLs, applies the same normalization to known secrets, and caps final text at300 characters. Independent fixtures cover MQTT/HTTPS credentials and embedded-control bypass. React renders this reason as text; HTML-like text is not interpreted as markup. This does not guarantee removal of arbitrary unknown credentials or personal data.

POST, explicit recheck and follow-up state request use the15second helper boundary, including asynchronous response-body waiting. Abort/timeout does not establish remote cancellation: an unknown receipt retains the original ID and is never automatically rePOSTed. HTTP status errors do not parse/reflect response bodies or synthesize accepted/rejected command outcomes;401 preserves authentication callback. Body syntax failures, identity mismatches and unknown statuses remain unconfirmed. Exact commandId/plantId/action/REST source binding matches current server early-rejection contracts, which may lack runId or request details.

App-lifetime manager preserves synchronous per-RTU pending guard across component remounts, allows other RTUs, and releases the guard after POST rather than waiting for state refresh. Confirmed command results survive failed follow-up GET. The explicit single-flight recheck cannot treat recent-list absence as proof of nonacceptance, erase confirmed results, regress executing/terminal observations, mix RTUs, or resurrect an evicted record. State-refresh generation and per-command recheck guards were covered by isolated fixtures. The UI displays captured RTU identity, distinct accepted/executing/completed or failure states, previous uncertainty, and memory-only recent20 scope. These are source/fixture findings; actual clicks/Enter/remount behavior is assigned to browser verification.

Residual limits are deliberate: there is no persistent journal, multi-tab exactly-once guarantee or remote operation cancellation. Browser timers can be delayed while the host is suspended. Response JSON comes from the authenticated same-origin application and is not newly given a streaming byte cap here;300-character displayed reason bounds retained text, not total response parse allocation. Generation guards order this receipt manager's follow-up requests, not all independent SSE/API state feeds. The historical proposal REVIEW024 remains proposal-time context; adopted PRD1.12 governs the candidate. No additional blocking finding remained in the reviewed revision.
