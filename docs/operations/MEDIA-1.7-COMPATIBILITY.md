# 제품1.7 후보 미디어 호환성 준비

IDEA011/PRD1.14, runtime d98d3c48d22c54e7fd092ee9e46ca9ab49e4bbf9의 미디어 준비 검토다. facts.template 선택은1.6.0/PRD1.13이며 미검수 보호값을 유지한다. 기존 안정1.6이나 최종 매니페스트를 대체하지 않는다.

바인더는 정확한1.7.0을 추가 지원하며1.7.1/1.7.0-rc.1/1.8.0을 거절한다.1.5~1.7의 접수 확인 장면을 유지한다. 정확한1.7의 commandListStatus 기능에서만 MQTT 장면의 초점을 제목 대신 `.command-list-state`로 이동하고 “목록의 마지막 확인 시각은 브라우저 조회 시각이며 명령 완료 시각과 다릅니다.”를 추가한다. 음성 시간 여유로6초를 추가했고 기존8장면·CSV 확대·REST접수 조회·RTU 양쪽 지표·별도MQTT125 흐름은 유지한다. 이전 버전의 장면 조작과 음성은 바꾸지 않는다.

로컬3111의 실제 API1.7.0 및 servedJS SHA154fe2de1f039fe27fbcce6793e15716e843ca3814c2d361392a2207b6918dfc를 확인했다. 기존 시험 RTU의1행 및5행 명령 목록을 읽기 전용으로 확인했고 POST/PATCH/DELETE 요청은0이었다.5행 대상에서 상태 영역은 y525~612, 명령 표는 y627~937, 오른쪽 경계1850으로1920×1080 안에 들어온다. RTU 메트릭의 양쪽 값도 보인다. 원본 결과와 실제 화면은 `artifacts/media-preparation/candidate17-layout/`에 보존한다.

이 조회 세션에는 새 REST receipt가 없어 이전 전체 리허설의 접수 패널 높이까지 재현한 것은 아니다. 새로운5행목록과 자막을 합성한 실제 녹화는 아직 수행하지 않았다. 따라서 전체리허설 전에 메인에 현재 구도·버전검사 결과를 보고한다. 다음 실제 녹화가 승인되면 전용RTU/사전기준/종료복원을 포함하는 기존 전체흐름에서 최종자막과 receipt/MQTT 표가 함께 보이는지 검수해야 한다.

관련 바인더45개와 장면5개, 총50개 집중 검사가 통과했다.1.7 전용 초점·설명, 이전 버전 분기, 명시적 허용버전/거절버전을 포함한다. 이 단계는 명령 목록의 정상 조회 화면만 관측했으며 네트워크 장애, 조회 실패 뒤의 과거 값 유지, 실제 운영 VPP 수신을 시연했다고 주장하지 않는다. 전체 SSE 연결 상태·목록 조회 상태·명령 완료 상태는 별도다. 음성 청취·최종 영상·PPT 검수도 수행하지 않았다. 원래 동결 이후 새 최종 캡처 의무는 유지한다.

## 실제 전체8장면 리허설 결과

메인의 실행 지시 후 로컬3111에서 새 전용 복합RTU를 실제UI201로 등록하여 전체8장면을 실행했다. 기존7개RTU는 읽기·선택만 했고 녹화 종료 후 모두 설정 해시가 같았다. 정상 복원 PASS 시각은2026-09-21T16:42:22.438Z이며 recorder58112는 exit0으로 종료했다.43개 소스 사본을 녹화 전에 보존했고 녹화 도중 소스는 바꾸지 않았다.

영상255.33초, 전체 제작279.911초,1080pH264/AAC 전체 디코딩 성공, 페이지 오류0이다.8개 인코딩 대표 장면과 접수·MQTT완료·새목록설명 자막 프레임을 직접 확인했다. 새목록 조회 안내/브라우저 확인 시각·REST접수 패널·MQTT125/125 completed·내보내기 버튼이 함께 보이며 최상단 목표값 행은 자막에 가리지 않는다. 실제 MQTT accepted/executing/completed 및 실제125kW/오차0, 동일RTU5개 상태내보내기를 검증했다.32개SRT 문장·시각은 원고/타임라인과 반올림 범위 내 일치한다. -19.2dB 음성은 기술 신호 확인이며 청취·발음 검수가 아니다.

준비 증거는 `artifacts/checkpoints/media-candidate17/`, 원본 영상/오디오는 `artifacts/media-preparation/full-candidate17-20260921/`에 있다. MP4 SHA-256은 `ccb890aa150f918d43cd8610147d5bd6a139c4b1664d1b71c82ef1adfc3acace`다. 정지 명령은 뒤 기동에 대체됐고 명령목록HTTP 실패를 시연한 것은 아니다. 외부KMA/AWS/운영VPP는 미검증이며 최종 영상으로 재사용하지 않는다. 최종 동결 이후 선택 릴리스로 새 녹화·검수·PPT가 필요하다.
