# 제품 검토 004

142개 ledger: local46, partial16, 이전 명령pass4, pending76. 상위 요구사항은 전체 하위 범위 검토 전 자동 승격하지 않는다. 최신 증거 SHA는 acceptance.json에 연결했다.

추가 검증: 실제55행 TSV/hybrid total, fractional startup Controller, 만료 후 model 불변, 실제 SIGKILL 실행명령 deadline 복구, 매 samples[] 발전기 집합, scenario active명령 취소,243497byte SSE100행/차트 갱신, 초기 대기/열린 무응답 SSE stale, 별도 backupDB pending본문 재전송/PUBACK/새75kW제어/복원 브라우저. 모두 해당 로컬 증거 범위로 반영했다.

복원 브라우저 authRequired=false의 authenticatedSSE:true는 과거 스크립트 필드명이 잘못된 것이다. 실제 상태 수신을 뜻하며 인증 검증으로 인정하지 않았다. 향후 스크립트는 false 인증 환경에서 null을 쓰는 것으로 확인했다.

우선 남은 인수 경계:
- 최종 이미지와 source/tests/doc/hash 일치, stable태그+일관 backup manifest, 원격 이전 정상 버전 rollback과 그 버전의 UI/MQTT/DB/outbox 복구.
- 실제 UI 제한/stop/start 후 차트·표·모델 일치, RTU 검색/선택 유지, 풍향→3D 반영, 복사/keyboard label, 누적 metric 프로세스 재시작+공유메모리 표기. 기록된 native UI 시험보다 넓은 상위 UI 요구는 개별로 대조한다.
- auth config exact 공개키·URL/요청/다운로드/로그 비노출의 전체 범위. 현재 correct/wrong token·독립tab·SSE·가이드 다운로드는 확인됨.
- 실제 KMA 키가 있으면 조회 기록, 없으면 미제공 조건을 명확히 기록. timeout·파싱·유효성·수동우선 fixtures는 확보됨.
- 보고서 옵션 없는 실행까지 검증됐으나 최종 suite/build/stable 승격 증거가 아직 필요함.
- 실제 마지막30분 영상·음성·자막·마우스/확대/전체decode와 후속PPT 전체슬라이드 render/notes/상대링크 인도.

제품 결함 추가 근거는 이번 회차에서 발견하지 못했다. 신규 기능 확장보다 최종 배포 이미지와 검증 기록의 일치 및 복원 게이트를 우선한다.
