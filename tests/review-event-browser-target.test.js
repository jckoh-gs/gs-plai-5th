import test from'node:test';import assert from'node:assert/strict';import{createRequire}from'node:module';const{fixtureConfig}=createRequire(import.meta.url)('../scripts/browser-event-time.cjs');const valid={GRID_FIXTURE_TARGET:'candidate',GRID_CANDIDATE_VERSION:'1.6.0',GRID_URL:'http://127.0.0.1:3110',EXPECTED_BUNDLE_SHA256:'a'.repeat(64),API_TOKEN_FILE:'artifacts/private/candidate-1.6/api-token',QA_DIRECTORY:'artifacts/checkpoints/event-target-test'};
test('readonly event harness requires explicit matching reviewed target before I/O',()=>{assert.equal(fixtureConfig(valid).base,valid.GRID_URL);for(const mode of ['main','isolated-restore'])assert.throws(()=>fixtureConfig({...valid,GRID_FIXTURE_TARGET:mode}),/mismatch/);for(const url of ['http://127.0.0.1:3104','http://user:secret@127.0.0.1:3110','http://example.invalid:3110','http://127.0.0.1:3110/'])assert.throws(()=>fixtureConfig({...valid,GRID_URL:url}),/mismatch/);assert.equal(fixtureConfig({...valid,GRID_FIXTURE_TARGET:'main',GRID_URL:'http://127.0.0.1:3104'}).mode,'main');assert.equal(fixtureConfig({...valid,GRID_FIXTURE_TARGET:'isolated-restore',GRID_URL:'http://127.0.0.1:3105'}).mode,'isolated-restore')});
test('event source gate refuses missing version/hash/token/output and unsafe path',()=>{for(const key of Object.keys(valid))assert.throws(()=>fixtureConfig({...valid,[key]:undefined}));for(const hash of ['A'.repeat(64),'a'.repeat(63)])assert.throws(()=>fixtureConfig({...valid,EXPECTED_BUNDLE_SHA256:hash}));assert.throws(()=>fixtureConfig({...valid,API_TOKEN_FILE:'artifacts/private/../../public'}));assert.throws(()=>fixtureConfig({...valid,GRID_CANDIDATE_VERSION:'1.5.0'}))});
