import test from 'node:test';import assert from 'node:assert/strict';import {eventTime} from '../web/event-time.js';
import {createRequire} from 'node:module';const require=createRequire(import.meta.url),{fixtureConfig}=require('../scripts/browser-event-time.cjs');
test('malicious timestamp and fallback contents are never returned or coerced',()=>{for(const created of ['Bearer private-fixture','2026-09-21T00:00:00.000Z<script>','2026-09-21T00:00:00.000Z\u0000',{toString(){throw Error('coerced')}}]){const result=eventTime({created,timestamp:'secret-fixture'});assert.equal(result,null);}});
test('fixture requires exact explicit read target version hash and private token path',()=>{const env={GRID_FIXTURE_TARGET:'candidate',GRID_CANDIDATE_VERSION:'1.6.0',GRID_URL:'http://127.0.0.1:3110',EXPECTED_BUNDLE_SHA256:'a'.repeat(64),API_TOKEN_FILE:'artifacts/private/test-token',QA_DIRECTORY:'artifacts/checkpoints/fixture'};assert.equal(fixtureConfig(env).mode,'candidate');for(const change of [{GRID_URL:'http://127.0.0.1:3104'},{GRID_URL:'https://external.invalid'},{GRID_CANDIDATE_VERSION:'1.5.0'},{API_TOKEN_FILE:'artifacts/private/../../token'},{GRID_FIXTURE_TARGET:''},{EXPECTED_BUNDLE_SHA256:''}])assert.throws(()=>fixtureConfig({...env,...change}));});

test('event regression candidate1.7 remains read-only and requires its exact3111 target',()=>{
 const env={GRID_FIXTURE_TARGET:'candidate',GRID_CANDIDATE_VERSION:'1.7.0',GRID_URL:'http://127.0.0.1:3111',EXPECTED_BUNDLE_SHA256:'a'.repeat(64),API_TOKEN_FILE:'artifacts/private/test-token',QA_DIRECTORY:'artifacts/checkpoints/fixture'};assert.equal(fixtureConfig(env).base,'http://127.0.0.1:3111');for(const change of [{GRID_URL:'http://127.0.0.1:3110'},{GRID_URL:'http://127.0.0.1:3104'},{GRID_CANDIDATE_VERSION:'1.8.0'}])assert.throws(()=>fixtureConfig({...env,...change}));
});
