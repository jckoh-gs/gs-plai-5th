import test from 'node:test';import assert from 'node:assert/strict';import {observationBindings} from '../scripts/observation-bindings.mjs';import {resumeDecision} from '../scripts/resume-policy.mjs';
test('malformed supervisor metadata returns fixed issues without raw values or exceptions',()=>{for(const state of [42,{},[],null]){let result;assert.doesNotThrow(()=>result=observationBindings({soak:{}},{supervisorDiagnosticUpgrade:{state},supplementaryObservation:{processIdentity:'PRIVATE_FIXTURE'}}));assert.equal(result.ok,false);assert(!JSON.stringify(result).includes('PRIVATE_FIXTURE'));const b={now:3000,freezeAt:new Date(1000).toISOString(),deadlineAt:new Date(2000).toISOString(),observationBindingsMatched:result.ok};assert.equal(resumeDecision(b),'deadline-report-only');}});
