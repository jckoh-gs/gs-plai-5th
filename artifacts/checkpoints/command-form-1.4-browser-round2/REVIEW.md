# Local command-form acceptance

PASS43 recorded checks /5 actual command POSTs against isolated3107 API1.4.0 and served `index-BndL-kls.js` (SHA256 in result.json). Runtime UI source did not change during these browser attempts. `source/` preserves the exact successful round2 harness and UI sources; the working harness later gained one direct-correction precondition, covered separately below.

Range errors were submitted via both button and Enter with zero command POSTs. Empty and overflow typed inputs for all5 fields, invalid Date1e20, valid extended-year Date, linked field alerts, focus, narrow layout, and RTU transition were checked. Min/max/decimal checks explicitly marked boundary-only did not claim actual commands for every endpoint value. A normal fractional command retained25.5/.125/3.25/50.5 and completed25.5kW. Actual local MQTT receipts—not fabricated REST polling phases—show accepted/executing/completed. REST polling observed accepted/completed only. Target0 completed while the generator remained on;200kW against100kW availability/rated1000 timed_out. Zero validity expired, and1001kW was rejected by the unchanged server rating constraint.

The prior directories `command-form-1.4-browser` and `command-form-1.4-browser-retry1` preserve failed harness attempts. The first expected transient executing from REST polling; the second checked broker messages too early despite injected1000ms latency. The final harness waits boundedly for actual broker receipts. Those failures do not claim completed acceptance. Exact pre-edit harness snapshots were not captured; UI sources remained unchanged. No failed logs or screenshots were replaced.

The additional `command-form-1.4-correction-delta` uses the successful round2 owned RTU only. It proves direct same-form1e20 error→30.5second correction→Enter→actual25.25kW completion, then .25second input produces a250ms expiry offset and a real server response without browser exceptions. It does not repeat all43 checks or touch remote/other RTUs. This fills the distinction between clearing errors by RTU switch and correcting the same form.

Existing independent-submit behavior remains: repeated submissions create distinct command IDs. Duplicate-submit prevention was not added or claimed; server same-ID idempotency is a separate contract. Helper tests5PASS and browser pageerror/unhandledrejection0. Broader MQTT/preview/export/scenario/backup/deployment gates belong to root's integration evidence. This is local feature acceptance, not final goal/media/k3s acceptance.
